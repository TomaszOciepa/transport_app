<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
      <div class="card shadow-lg rounded-4 border-0">
        <div class="card-body p-5">
          <h2 class="mb-4 fw-bold text-center">Rejestracja</h2>

          <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "needs-validation" }) do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>

            <div class="mb-3">
              <%= f.label :email, "Email", class: "form-label fw-bold" %>
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control form-control-lg", placeholder: "Twój email", required: true %>
            </div>

            <div class="mb-3">
              <%= f.label :password, "Hasło", class: "form-label fw-bold" %>
              <% if @minimum_password_length %>
                <small class="text-muted">(<%= @minimum_password_length %> znaków minimum)</small>
              <% end %>
              <%= f.password_field :password, autocomplete: "new-password", class: "form-control form-control-lg", placeholder: "Hasło", required: true %>
            </div>

            <div class="mb-3">
              <%= f.label :password_confirmation, "Potwierdź hasło", class: "form-label fw-bold" %>
              <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control form-control-lg", placeholder: "Potwierdź hasło", required: true %>
            </div>

            <div class="d-flex justify-content-center mt-4">
              <%= f.submit "Zarejestruj się", class: "btn btn-dark rounded-pill px-5 py-2 shadow-sm" %>
            </div>
          <% end %>

          <div class="text-center mt-3">
            <%= render "devise/shared/links" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
